language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-hexe-ruby-amqp' ]; then cd .. && eval "mv $currentfolder generator-hexe-ruby-amqp" && cd generator-hexe-ruby-amqp; fi
